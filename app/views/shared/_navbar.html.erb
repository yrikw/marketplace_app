<nav class="navbar navbar-expand-lg navbar-light p-5 sticky-top text-left">

<div class="container">
  <%= link_to "Share Garden", root_path, class: "apptitle link" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item">
         <%= link_to "Shop", listings_path, class: "nav-link" %>
      </li>

      <% if user_signed_in? %>

      <li  class="nav-item">
      <% if current_user.profile.present? %>
        <%= link_to 'My Profile', current_user.profile, class: 'nav-link' %>
     <% else %>
        <%= link_to 'Create profile', new_profile_path, class: 'nav-link'  %>
      <% end %>
      </li>

      <li class="nav-item">
          <% if current_user.profile.present? %>
            <%= link_to "Share", new_listing_path, class: "nav-link" %>
          <% end %>
        </li>
      </ul>

      <ul class="navbar-nav">
      <li class="nav-item row ">
      <i class="fas fa-user-circle mt-2 mr-2">   </i>
         <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "nav-link" %>
      </li>

      <% else %>

      <li class="nav-item row">
        <i class="fas fa-user-circle mt-2 mr-2">  </i>
         <%= link_to "Log In", new_user_session_path, class: "nav-link" %>
      </li>
      </ul>
      <% end %>
 </div>

</nav>